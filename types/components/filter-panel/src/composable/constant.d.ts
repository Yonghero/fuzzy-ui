export function mapperToOptions(mapper: any): {
    label: string;
    value: any;
}[];
export namespace LOGICAL_MAPPER {
    let 且: string;
    let 或: string;
}
export namespace RELATION_DIGIT_MAPPER {
    let 等于: string;
    let 大于: string;
    let 小于: string;
    let 介于: string;
    let 为空: string;
    let 非空: string;
}
export namespace RELATION_MAPPER {
    export let 属于: string;
    export let 不属于: string;
    let 为空_1: string;
    export { 为空_1 as 为空 };
    let 非空_1: string;
    export { 非空_1 as 非空 };
}
//# sourceMappingURL=constant.d.ts.map